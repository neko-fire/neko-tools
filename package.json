{
  "name": "toolkit-desktop",
  "version": "0.1.0",
  "author": "Martin Bayer",
  "description": "A local desktop toolkit for time conversion and UUID generation.",
  "private": true,
  "scripts": {
    "dev": "open -a Safari static/index.html",
    "test": "node --test \"tests/*.test.js\"",
    "build": "scripts/build-app.sh",
    "dmg": "scripts/build-dmg.sh",
    "start": "open dist/Toolkit.app",
    "package:mac": "npm run build && npm test && npm run dmg"
  }
}
